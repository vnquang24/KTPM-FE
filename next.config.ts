import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      'placehold.co', 
      'images.unsplash.com', 
      'picsum.photos', 
      'source.unsplash.com',
      'i.pravatar.cc'
    ],
  },
};

export default nextConfig;
